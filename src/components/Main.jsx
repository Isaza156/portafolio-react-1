import React from 'react';

const Main = ({ children }) => (
    <div className="Main">
        <div className="Main__container">
            {children}
        </div>
    </div>
);
export default Main;