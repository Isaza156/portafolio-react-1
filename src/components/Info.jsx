import React from 'react';

const Info = ({ children }) => (
    <div className="Info">
        <div className="Info__container">
            {children}
        </div>
    </div>
);
export default Info;